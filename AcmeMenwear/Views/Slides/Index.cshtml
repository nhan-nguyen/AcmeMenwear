@model IEnumerable<AcmeMenwear.Models.Slide>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SlideLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SlideDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SlideLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SlideDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SlideId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SlideId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SlideId })
        </td>
    </tr>
}

</table>
